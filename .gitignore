# -----------------------------------------------------------------------------
# CORE DRUPAL & COMPOSER EXCLUSIONS
# These are files and directories that should not be tracked by Git,
# as they are managed by Composer or are part of Drupal core.
# -----------------------------------------------------------------------------

# Composer-managed directories and components:
/vendor/
/web/core/
/web/modules/contrib/
/web/themes/contrib/
/web/profiles/contrib/
/web/libraries/
/drush/contrib/

# The main top-level Composer dependency directories and files:
/autoload.php
/console

# -----------------------------------------------------------------------------
# GENERATED AND BUILD FILES
# These are files created during runtime, installation, or build processes.
# -----------------------------------------------------------------------------
/web/themes/custom/carbonv1/assets

# Private and public file system directories:
/web/sites/*/files/
!/web/sites/*/files/.gitkeep
/private/
!/private/.gitkeep

# Local settings and service definitions (environment-specific):
/web/sites/*/settings.local.php
/web/sites/*/services.local.yml

# Drupal configuration for developer-only modules (e.g., Devel):
/config/sync/devel.*

# Node dependencies and logs:
node_modules/
npm-debug.log

# -----------------------------------------------------------------------------
# LOCAL ENVIRONMENT & TOOLING
# Ignore files related to local development environments (DDEV, Docksal, etc.)
# -----------------------------------------------------------------------------
.env
.ddev/
.docksal/
.idea/
.vscode/
*.log
auth.json
keys/
docker-compose.yml
/web.config

# -----------------------------------------------------------------------------
# OPERATING SYSTEM & IDE FILES
# Common files generated by OS and IDEs that should be ignored.
# -----------------------------------------------------------------------------
*.DS_Store
*.swp
.buildpath
.project
.settings/
/nbproject/