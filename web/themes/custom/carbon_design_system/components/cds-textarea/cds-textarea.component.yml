name: Carbon Textarea
description: IBM Carbon textarea (Web Component)
status: stable
 
props:
  type: object
  properties:
    id:           { type: string }
    name:         { type: string }
    label:        { type: string, title: Label }
    value:        { type: string, default: '' }
    placeholder:  { type: string, default: '' }
    helper_text:  { type: string, default: '' }
    rows:         { type: integer, default: 4, minimum: 1 }
    size:         { type: string, default: md, enum: [sm, md, lg] }
    disabled:     { type: boolean, default: false }
    required:     { type: boolean, default: false }
    readonly:     { type: boolean, default: false }
    invalid:      { type: boolean, default: false }
    invalid_text: { type: string, default: '' }
  required: [label]